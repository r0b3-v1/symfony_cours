{% include "header.html.twig" %}
<div class="container d-flex flex-direction-column mt-5">

	<table>
		<thead>
			<tr>
				<th>{{contact.prenom}}</th>
			</tr>
		</thead>
		<tr>
			<td>nom :{{contact.nom}}</td>
			<td>numéro :
				{{contact.numTel}}</td>
		</tr>
		{% for adresse in contact.adresses %}
			<tr>
				<td>{{adresse.nom}}</td>
			</tr>
		{% endfor %}
	</table>
</div>

{% include 'footer.html.twig' %}
